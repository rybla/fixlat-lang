{
  "dependencies": {
    "fs": "^0.0.1-security",
    "uuid": "^9.0.0"
  },
  "scripts": {
    "build": "npx spago build --source-maps",
    "test": "rm -f querying-snapshot*.html && npm run build && npx spago test",
    "run": "npm run build && npx spago run"
  },
  "devDependencies": {
    "live-serve": "^0.0.0",
    "purescript": "^0.15.8",
    "spago": "^0.20.9"
  }
}
